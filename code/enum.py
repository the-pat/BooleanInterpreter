#!/usr/bin/python

# Contains the different types available for the boolean language
class Enum:
    EOF = "EOF"
    BOOL = "BOOL"
    IMPLY = "IMPLY"
    OR = "OR"
    AND = "AND"
    NOT = "NOT"
    WHITESPACE = "WHITESPACE"
    PERIOD = "PERIOD"
    OPEN_PAREN = "OPEN_PAREN"
    CLOSED_PAREN = "CLOSED_PAREN"
